enum PostType {
  addPost,
  editPost,
}

enum MapFlowType {
  people,
  places,
}

enum AccessType {
  authorized,
  guest,
}

enum LoadingState {
  loading,
  active,
  error,
}

enum MarketplaceProductType {
  services,
  products,
  none,
}

enum ProfileType {
  human,
  pet,
  none,
}