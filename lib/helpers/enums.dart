enum PostType {
  addPost,
  editPost,
}

enum MapFlowType {
  people,
  places,
}

enum AccessType {
  authorized,
  guest,
}

enum LoadingState {
  loading,
  active,
  error,
}

enum MarketplaceState {
  services,
  products
}